# Environment Variables
# Copy this file to .env.local and update with your values

# Node environment
NODE_ENV=development

# Application URL (used for API calls, OAuth redirects, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000
